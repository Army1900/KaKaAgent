# 项目目录理解

- name: 项目目录理解
- summary: 读取目录画像、入口文件和项目类型，作为会话上下文。
- permission: read
- triggers: 目录,项目,代码,文件,页面,样式,bug,运行

## 使用方式

当用户绑定目录后，先建立项目画像：

1. 识别项目类型。
2. 统计文件和文件夹数量。
3. 找 README、package.json、入口文件等线索。
4. 给出当前任务可能需要读取的文件范围。
5. 不主动写入，不主动执行命令。
